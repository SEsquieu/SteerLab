server {
  listen 443 ssl;
  server_name accounts.internal;

  client_max_body_size 1m;

  location /api/account/ {
    proxy_pass http://account-service;
    proxy_set_header Host $host;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  }
}
